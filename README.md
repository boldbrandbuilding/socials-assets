# BBB Socials — Public Asset Host

Public image storage for the BBB daily Socials pipeline. Contains only rendered PNGs — no code, no credentials. Images are served via raw.githubusercontent.com URLs for Meta's Content Publishing API, which requires a public URL to fetch from.
